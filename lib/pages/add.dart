import 'package:flutter/material.dart';

class AddToPage extends StatelessWidget {
  const AddToPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Container(),
    );
  }
}
